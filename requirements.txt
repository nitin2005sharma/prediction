# Core (also declared in pyproject.toml -- kept here too since many
# recruiters/CI systems still expect a plain requirements.txt)
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.12
numpy>=1.24
joblib>=1.3

# Needed only for the Streamlit demo app (src/student_performance/app.py).
# Listed here (not just as an extra) because Streamlit Community Cloud
# installs straight from this file.
streamlit>=1.30

# Install with: pip install -r requirements.txt
# Or, for the packaged CLI entry points (sp-train, sp-predict, sp-visualize):
#   pip install -e .
# For the Streamlit demo app too:
#   pip install -e ".[app]"
